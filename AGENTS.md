# Project Environment

- Use the Conda environment `dl` for this project.
- Run Python commands, tests, training, and inference inside `dl`, for example:
  - `conda run -n dl python -m pytest`
  - `conda run -n dl python -m src.train_siglip2_head`
- When an interactive shell is needed, activate it first with `conda activate dl`.
