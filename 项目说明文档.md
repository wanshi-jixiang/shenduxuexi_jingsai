# 图像情感识别竞赛项目说明文档

---

## 一、整体说明

### 1.1 项目背景

图像情感识别（Image Emotion Recognition）是计算机视觉与情感计算交叉领域的核心研究方向之一。该任务旨在使计算机能够自动识别并理解图像所传递的人类情感，其本质是一个多类别视觉语义理解问题。与狭义的人脸表情识别不同，图像情感分类需要综合考量画面中的主体特征、场景氛围、色彩倾向、构图方式及上下文语义等多重因素，对模型的抽象推理能力提出了更高要求。

### 1.2 核心任务

本项目要求参赛者基于给定的图像数据集，设计并实现一个深度学习模型，自动将输入图像分类至 8 种离散情感类别之一。任务涵盖完整的机器学习工程链路，包括数据审计、训练/验证集划分、输入预处理、模型训练、验证评估、测试集推理以及结果提交。

### 1.3 应用场景

图像情感识别技术在多个领域具有广泛的实际应用价值：

- **智能内容推荐**：根据图像情感属性为用户推荐符合其当前情绪偏好的图片、视频或广告内容。
- **心理健康监测**：辅助分析用户在社交媒体上发布的图像内容，识别潜在的负面情绪倾向。
- **数字艺术创作**：为艺术图像自动标注情感标签，支持基于情感语义的艺术品检索与策展。
- **人机交互系统**：使智能设备能够感知并响应视觉环境中的情感信息，提升交互自然度。

---

## 二、竞赛目标

### 2.1 具体问题

参赛者需解决的核心问题为：**在给定 3018 张标注图像上训练分类模型，使其能够对 1391 张未标注测试图像进行准确的 8 类情感预测。**

具体而言，模型需具备以下能力：

1. **细粒度情感理解**：区分视觉上相近的情感类别（如 `contentment` 与 `amusement`、`fear` 与 `sadness`）。
2. **跨品类泛化**：正确处理包含人物、风景、动物、静物、艺术画等多种内容形态的图像。
3. **长尾类别识别**：在类别分布严重不均衡的条件下，避免模型向高频类别偏置，保证少数类的识别召回率。

### 2.2 模型评估指标

本竞赛采用 **Macro-F1（宏平均 F1 分数）** 作为主要评估指标，**Accuracy（准确率）** 作为辅助参考指标。

#### 2.2.1 Macro-F1（主指标）

Macro-F1 对所有类别赋予同等权重，先分别计算每个类别的精确率（Precision）和召回率（Recall），再取平均得到宏平均 F1 值。其计算公式如下：

$$
\text{Macro-F1} = \frac{1}{C} \sum_{i=1}^{C} \frac{2 \cdot P_i \cdot R_i}{P_i + R_i}
$$

其中，$C = 8$ 为类别总数，$P_i$ 和 $R_i$ 分别为第 $i$ 类的精确率和召回率。

**选取理由**：由于训练数据类别分布显著不均衡（最大类别约为最小类别的 7.2 倍），Macro-F1 能够有效度量模型在所有类别上的均衡表现，避免因模型偏向高频类别而获得虚高的整体准确率。该指标对少数类的识别性能具有强敏感性，与竞赛核心难点高度契合。

#### 2.2.2 Accuracy（辅助指标）

$$
\text{Accuracy} = \frac{\text{预测正确的样本数}}{\text{总样本数}}
$$

Accuracy 反映模型的整体分类正确率，但不区分各类别的贡献差异。在类别不均衡场景下，Accuracy 可能掩盖少数类的识别缺陷，因此仅作为辅助观察指标，不用于最终排名。

#### 2.2.3 类别级指标

在模型开发阶段，建议同步监控每类的 Precision、Recall 及 F1-score，并输出混淆矩阵（Confusion Matrix），以定位易混淆类别对（如 `contentment` 与 `amusement`）及系统性偏置问题。

### 2.3 提交要求

参赛者需生成名为 `test.txt` 的预测结果文件，格式要求如下：

- **文件编码**：UTF-8 无 BOM。
- **内容格式**：每行一条记录，为一个预测情感标签。
- **顺序要求**：行顺序必须严格对应测试集图片的数字自然序，即第 1 行对应 `1.jpg`，第 2 行对应 `2.jpg`，以此类推，直至第 1391 行对应 `1391.jpg`。
- **合法标签**：仅允许以下 8 个类别名称之一：`amusement`、`anger`、`awe`、`contentment`、`disgust`、`excitement`、`fear`、`sadness`。
- **行数校验**：文件必须恰好包含 1391 行，不得有空行或多余空格。

提交前建议生成并保留两种格式的对照文件（纯标签版与"文件名 标签"版），以应对可能的格式歧义。

---

## 三、数据集结构

### 3.1 总体概况

数据集分为训练集与测试集两个部分，整体结构如下：

```
dataset/
├── train/
│   └── train/
│       ├── amusement/       # 444 张图像
│       ├── anger/           # 588 张图像
│       ├── awe/             # 606 张图像
│       ├── contentment/     # 294 张图像
│       ├── disgust/         # 84 张图像
│       ├── excitement/      # 222 张图像
│       ├── fear/            # 390 张图像
│       └── sadness/         # 390 张图像
├── test/
│   └── test/
│       ├── 1.jpg
│       ├── 2.jpg
│       ├── ...
│       └── 1391.jpg
└── test.txt                 # 提交示例文件（纯标签格式）
```

### 3.2 训练集（Training Set）

#### 3.2.1 规模与划分

训练集共包含 **3,018 张** 已标注图像，覆盖 **8 个** 情感类别。参赛者需自行将其划分为训练子集（Train Split）和验证子集（Validation Split）。推荐采用 **分层 8:2 划分**（Stratified 80/20 Split），确保每个类别在训练子集与验证子集中的比例分布与原始分布保持一致，并使用固定随机种子以保证实验可复现。

#### 3.2.2 类别定义与样本分布

| 序号 | 类别名称（英文） | 类别语义 | 样本数量 | 占比 |
|:---:|:---|:---|:---:|:---:|
| 1 | amusement | 愉悦、 amusement、娱乐感 | 444 | 14.7% |
| 2 | anger | 愤怒、恼怒 | 588 | 19.5% |
| 3 | awe | 敬畏、惊叹 | 606 | 20.1% |
| 4 | contentment | 满足、安宁 | 294 | 9.7% |
| 5 | disgust | 厌恶、反感 | 84 | 2.8% |
| 6 | excitement | 兴奋、激动 | 222 | 7.4% |
| 7 | fear | 恐惧、害怕 | 390 | 12.9% |
| 8 | sadness | 悲伤、哀伤 | 390 | 12.9% |
| **合计** | — | — | **3,018** | **100%** |

**关键观察**：类别分布呈现显著的长尾特征。`awe` 类样本最多（606 张），`disgust` 类样本最少（仅 84 张），最大类别约为最小类别的 **7.2 倍**。这种不均衡性对模型训练构成核心挑战，若不加处理，模型极易偏向高频类别，导致少数类（尤其是 `disgust`）的召回率接近零，进而严重拉低 Macro-F1。

#### 3.2.3 目录组织与标注方式

训练集采用 **按类别分目录** 的结构化存储方式。每个子目录的名称即对应情感类别标签，目录内的图像文件均属于该类。这种组织方式天然提供了图像与标签的映射关系，便于使用基于文件夹的数据加载器（如 PyTorch 的 `ImageFolder`）直接读取。

图像文件名本身不包含类别信息，标签完全由所在目录决定。图像格式均为 JPG，但尺寸、宽高比及色彩模式存在差异（训练集涵盖 388 种不同尺寸，且存在少量灰度图像）。

### 3.3 测试集（Test Set）

#### 3.3.1 规模与用途

测试集共包含 **1,391 张** 未标注图像，用于评估模型的最终泛化性能。参赛者需使用训练好的模型对测试集中每张图像进行情感类别预测，并按指定格式输出结果。

#### 3.3.2 文件命名规范

测试图像采用数字序号命名，格式为 `<序号>.jpg`，序号从 **1** 连续递增到 **1391**。例如：

```
1.jpg, 2.jpg, 3.jpg, ..., 1390.jpg, 1391.jpg
```

**重要提示**：测试图像在文件系统中的物理存储顺序可能并非严格的数字升序（部分操作系统按字符串排序时会出现 `1.jpg, 10.jpg, 100.jpg` 的错位）。推理阶段必须按 **数字自然序**（Numerical Natural Order）读取和输出预测结果，以确保 `test.txt` 的第 $n$ 行准确对应 `n.jpg`。

#### 3.3.3 图像特征

测试集图像在尺寸分布上呈现高度多样性（测试集涵盖 256 种不同尺寸），宽高范围与训练集大致相仿。同时，测试集中存在部分灰度图像（单通道），推理前必须统一转换为 RGB 三通道格式，否则会导致模型输入维度不匹配。

### 3.4 数据字段说明

| 字段 | 位置 | 数据类型 | 说明 |
|:---|:---|:---|:---|
| 图像文件 | `dataset/train/train/<类别>/` | JPG 图像 | 训练样本，像素尺寸不统一 |
| 类别标签 | 目录名 | 字符串 | 8 个合法类别之一，作为训练监督信号 |
| 测试图像 | `dataset/test/test/` | JPG 图像 | 待预测的未标注样本 |
| 预测结果 | `test.txt` | 字符串列表 | 每行一个预测标签，共 1391 行 |

### 3.5 数据关联与使用规范

1. **训练-验证关联**：训练集与验证集均从同一 `train` 目录下的 3,018 张图像中划分而来。所有实验模型必须共用同一划分策略与随机种子，以确保不同模型间的验证结果具有可比性。

2. **训练-测试关联**：测试集与训练集在图像内容来源上同源同质，但测试集完全不提供任何标签信息。严禁以任何形式利用测试集标签进行训练或调参（包括伪标签策略仅在最终冲刺阶段谨慎尝试，且必须基于独立验证收益决策）。

3. **输入预处理一致性**：训练/验证阶段使用的图像预处理（归一化、尺寸调整、数据增强）必须与测试推理阶段完全一致，避免因预处理分布偏移导致的性能损失。若使用 OpenCLIP 等预训练模型，必须采用其官方配套的预处理变换。

4. **通道格式统一**：所有图像在加载阶段必须统一转换为 RGB 格式。灰度图直接复制三通道即可，确保输入张量维度始终为 `[Batch, 3, H, W]`。

---

## 四、附录

### 4.1 情感类别语义参考

为便于理解标签口径，对各类别的视觉语义特征作如下补充说明：

- **amusement**：画面中常包含笑容、轻松互动或喜剧性元素。
- **anger**：常伴随皱眉、激烈动作、冲突场景或强烈的色彩对比。
- **awe**：通常表现为壮丽的自然景观、宏伟建筑或令人屏息的视觉奇观。
- **contentment**：氛围宁静祥和，常表现为安逸的人物状态或温馨场景。
- **disgust**：画面可能包含令人不适的物质、扭曲表情或厌恶姿态。
- **excitement**：动态感强，常表现为欢呼、跳跃或高度活跃的场景。
- **fear**：常包含惊悚元素、紧张姿态、黑暗压抑的场景或威胁性构图。
- **sadness**：色调偏冷，常包含孤独的人物、离别场景或衰败意象。

### 4.2 关键风险提醒

| 风险项 | 描述 | 建议措施 |
|:---|:---|:---|
| 类别不均衡 | `disgust` 仅 84 张，易导致模型忽略该类 | 使用类权重交叉熵、WeightedRandomSampler 或 Focal Loss |
| 测试排序错位 | 字符串排序与数字自然序不一致 | 读取时按整数序号排序，而非默认字符串排序 |
| 灰度图异常 | 单通道图像导致维度错误 | 加载阶段统一调用 `.convert('RGB')` |
| 预处理混用 | 不同预训练模型使用不同的归一化参数 | OpenCLIP 用官方 `preprocess`，CNN 用 ImageNet 统计值 |
| 提交格式错误 | 行数、标签或顺序有误 | 提交前逐项核对检查清单 |

---

*文档版本：v1.0*  
*编制日期：2026 年 6 月 2 日*  
*适用范围：图像情感识别竞赛项目开发参考*
